# Test environment configuration for deployed app
# Copy this to .env.test.production and update with actual values

# Deployed application URL (update with actual Vercel URL)
PLAYWRIGHT_TEST_BASE_URL=https://italian-flashcards.vercel.app

# Supabase configuration (same as production)
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Test user credentials (created by create-test-user.js)
TEST_USER_EMAIL=test-e2e@example.com
TEST_USER_PASSWORD=TestPassword123!

# Optional: Skip tests if no real auth config
SKIP_REAL_AUTH_TESTS=false